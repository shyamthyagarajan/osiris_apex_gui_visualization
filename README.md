# Ephemeris GUI Visualizer

This project allows the user to select ephemeris bodies of their choice, along with a desired date range, and generate a plot animation of the orbits fixed in a heliocentric frame (relative to the Sun). The GUI interface also offers the user the ability to change colors of the body/trail and input a desired step size value to slow down or speed up the simulation.

## Installation

1. Clone the repository:
```bash
git clone https://github.com/shyamthyagarajan/ephemeris_gui_visualizer.git
```

2. Change working directory to the project:
```bash
cd ephemeris_gui_visualizer
```

3. Install dependencies:
```bash
pip install -r requirements.txt
```

4. Run the project:
```bash
python main.py
```

5. Make selections within the GUI interface (e.g. Apophis, Osiris-Apex, Earth rendezvous plot)
    ![Osiris_Apex Visualization](example_data_run/README.gif)